---
schema_version: '0.1'
type: source_card
source_card_id: src_20260614_rib045_09
created_at: '2026-06-14T12:00:00-04:00'
created_by_agent: rf-swarm-carder
sensitivity: personal
source:
  title: "logprobs - vllm.v1.engine.logprobs API Reference (vLLM Documentation)"
  source_type: official_doc
  locator: {url: "https://docs.vllm.ai/en/stable/api/vllm/v1/engine/logprobs/", file_path: null, doi: null, repo: "https://github.com/vllm-project/vllm"}
  authors: []
  publisher: "vLLM project (vllm-project)"
  published_at: null
  accessed_at: '2026-06-14T12:00:00-04:00'
  version: "v0.23.0 (stable docs)"
trust: {source_rank: primary, reliability_notes: "Official vLLM auto-generated API reference for the v1 engine logprobs module; mirrors the source of vllm/v1/engine/logprobs.py at the stable release.", known_limitations: ["This page is an auto-generated API/source dump for the LogprobsProcessor dataclass, NOT conceptual prose; it does not itself state the logprobs/prompt_logprobs sampling-parameter semantics, the max-logprobs cap, the -1 no-cap behavior, OOM risk, or the native-vs-OpenAI endpoint exposure attributed to vLLM (those belong to other vLLM docs). Only the docstrings and code shown on this page are quoted here."], conflicts_with: []}
usage: {allowed_for_public_output: false, allowed_for_work_output: true, allowed_for_personal_meatywiki: true, citation_required: true, quote_limit_notes: "Short excerpts only."}
extracted_points:
- evidence_id: ev_001
  locator: "Class header: vllm.v1.engine.logprobs > LogprobsProcessor (dataclass)"
  summary: "vLLM's v1 engine implements logprobs handling through a LogprobsProcessor dataclass that carries both per-output sample logprobs and prompt logprobs for a request."
  quote: "@dataclass\nclass LogprobsProcessor:"
  supports_potential_claims: [clm_pending]
- evidence_id: ev_002
  locator: "LogprobsProcessor dataclass fields"
  summary: "The LogprobsProcessor holds separate optional fields for sample logprobs and prompt logprobs (SampleLogprobs and PromptLogprobs), confirming vLLM tracks both the chosen-token output logprobs and input-prompt logprobs as distinct structures."
  quote: "# Logprobs for this request\n    logprobs: SampleLogprobs | None\n    prompt_logprobs: PromptLogprobs | None\n    cumulative_logprob: float | None\n    num_logprobs: int | None\n    num_prompt_logprobs: int | None"
  supports_potential_claims: [clm_pending]
- evidence_id: ev_003
  locator: "LogprobsProcessor.from_new_request classmethod"
  summary: "Whether sample logprobs and prompt logprobs are produced is driven by the request's sampling parameters: num_logprobs comes from sampling_params.num_logprobs and num_prompt_logprobs from sampling_params.prompt_logprobs, and each logprobs structure is only created when its count is not None."
  quote: "num_logprobs = sampling_params.num_logprobs\n        num_prompt_logprobs = sampling_params.prompt_logprobs"
  supports_potential_claims: [clm_pending]
- evidence_id: ev_004
  locator: "LogprobsProcessor._update_sample_logprobs docstring + body comment"
  summary: "For each generated position, vLLM's sampler places the actually-sampled token's logprob first in the logprobs list, and the processor accumulates it into the running cumulative_logprob."
  quote: "# Sampler puts the sampled logprob in first.\n            sampled_token_logprob = logprobs[0]\n            self.cumulative_logprob += sampled_token_logprob"
  supports_potential_claims: [clm_pending]
- evidence_id: ev_005
  locator: "LogprobsProcessor.pop_prompt_logprobs docstring"
  summary: "Prompt logprobs are aggregated across one or more prefill chunks and returned all at once at the end of prefill; pop_prompt_logprobs returns None if prompt logprobs are disabled for the request, otherwise the full list."
  quote: "The logprobs processor aggregates prompt chunk logprobs over one or more prefill chunks. This method returns all prompt logprobs at once and then forgets them."
  supports_potential_claims: [clm_pending]
- evidence_id: ev_006
  locator: "LogprobsProcessor._verify_tokens docstring (Args: tokens)"
  summary: "The token IDs returned at a given logprobs position are alternatives at the same position (sampled token plus top-k alternatives such as [sampled, top1, top2]), not sequential tokens, clarifying that vLLM returns the chosen token alongside its top-ranked alternatives per position."
  quote: "Token IDs corresponding to decoded_tokens_list.\n                These are alternatives at the SAME position (e.g.\n                [sampled, top1, top2]), NOT sequential tokens."
  supports_potential_claims: [clm_pending]
- evidence_id: ev_007
  locator: "LogprobsProcessor._correct_decoded_token docstring"
  summary: "vLLM applies UTF-8 correction when detokenizing logprob tokens: byte-fallback tokenization can split multi-byte UTF-8 characters across tokens, producing the U+FFFD replacement character, and the processor uses preceding sampled tokens as context to reconstruct correct text."
  quote: "When byte-fallback tokenization splits multi-byte UTF-8 characters across tokens, individual token decoding produces the replacement character U+FFFD. This method uses preceding sampled tokens as context to reconstruct the correct text."
  supports_potential_claims: [clm_pending]
---
# Source Card: logprobs - vllm.v1.engine.logprobs API Reference (vLLM Documentation)

## Summary
This is the official vLLM auto-generated API reference page for the `vllm.v1.engine.logprobs` module, documenting the `LogprobsProcessor` dataclass that the v1 engine uses to assemble logprobs. It shows, via docstrings and source, that vLLM tracks both sample (output) logprobs and prompt logprobs as separate structures, that their production is gated by the request's sampling parameters (`num_logprobs` / `prompt_logprobs`), that the sampled token's logprob is placed first and accumulated into a cumulative logprob, and that per-position results include top-k alternatives alongside the chosen token with UTF-8 detokenization correction.

## Key evidence
- (ev_001) vLLM v1 engine handles logprobs through a `LogprobsProcessor` dataclass.
- (ev_002) The processor stores sample logprobs and prompt logprobs as distinct optional fields.
- (ev_003) Logprobs production is driven by `sampling_params.num_logprobs` and `sampling_params.prompt_logprobs`, created only when not None.
- (ev_004) The sampler puts the chosen token's logprob first; it is summed into `cumulative_logprob`.
- (ev_005) Prompt logprobs are aggregated over prefill chunks and returned all at once at end of prefill; None if disabled.
- (ev_006) Per-position token IDs are same-position alternatives (`[sampled, top1, top2]`), i.e. chosen token plus top-k.
- (ev_007) UTF-8 correction reconstructs multi-byte characters split by byte-fallback tokenization (U+FFFD handling).

## Limitations
- This page is an auto-generated API/source dump for `LogprobsProcessor`, NOT conceptual prose. It does NOT itself state the higher-level claims attributed to vLLM in the task brief (the `logprobs` sampling param returning the N most likely tokens, `prompt_logprobs` echo-equivalence, a configurable `max-logprobs` cap, `-1` meaning no cap allowing `output_length * vocab_size` logprobs, OOM risk from uncapped logprobs, or exposure via both native and OpenAI-compatible endpoints). Those facts must be sourced from other vLLM docs (e.g. sampling params / OpenAI-compatible server pages) and are deliberately NOT quoted here to avoid fabrication.
- No publish date or named authors are shown; version inferred from the docs banner (v0.23.0). The page reflects the "stable" docs at access time and may change across releases.
