# Rate limiting for a multi-tenant API gateway — candidate synthesis (Gemini)

Cloud provider documentation groups rate-limiting approaches into token-bucket and sliding-window
families, with grounding chips pointing at each provider's own docs for their default limiter choice.
One further observation in this answer had no grounding chip attached at all — that span is preserved
as an ungrounded candidate below rather than being silently dropped or given a fabricated source.
