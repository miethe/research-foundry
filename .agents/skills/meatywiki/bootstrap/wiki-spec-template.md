---
name: meatywiki-bootstrap-wiki-spec-template
description: Canonical template for <vault>/wiki-spec.md generated by bootstrap step B5. Captures project intent, ingest sources, lens priorities, deferred items.
type: progressive-disclosure
skill_name: meatywiki
schema_version: 1
cli_version_range: "compilation-engine-v1 – v1.2 (pre-release)"
---

# `wiki-spec.md` Template

The block below is the **exact content** to write to `{{VAULT_PATH}}/wiki-spec.md`. Substitute `{{...}}` placeholders. The file is canonical for **project intent**; agents read it before making scope decisions (what to ingest, what to defer, which lenses to prioritize).

The template is intentionally sparse. Bootstrap **does not invent** ingest sources, lens priorities, or success criteria. The user fills these in over time.

---

## Template Body (copy verbatim, substitute placeholders)

````markdown
---
name: {{PROJECT_NAME}}-wiki-spec
description: Intent, scope, and operating conventions for the {{PROJECT_NAME}} knowledge vault.
project: {{PROJECT_NAME}}
vault_path: {{VAULT_PATH}}
bootstrapped_at: {{BOOTSTRAP_DATE}}
schema_version: "1.0.0"
status: draft
---

# {{PROJECT_NAME}} — Wiki Spec

## 1. Intent

{{INTENT_PROMPT}}

<!--
One paragraph. What is this vault FOR? Who reads it? What question does it answer
when an agent queries it six months from now? Keep it concrete; resist generalities.
-->

## 2. Scope

**In scope** — knowledge domains this vault covers:

- TODO: list 3–5 domains. Examples: "internal architecture decisions", "vendor evaluations", "meeting notes from project X".

**Out of scope** — things that explicitly belong elsewhere:

- TODO: list 2–3. Examples: "code documentation (lives in repo)", "personal notes", "anything covered by another vault".

## 3. Ingest Sources

What feeds raw/ on a regular basis. Bootstrap leaves this empty; populate as sources stabilize.

| Source type | Origin | Cadence | Notes |
|---|---|---|---|
| <!-- url --> | <!-- e.g., team blog --> | <!-- weekly --> | <!-- e.g., classify as concept --> |
| <!-- pdf --> | <!-- e.g., vendor whitepapers --> | <!-- ad-hoc --> | |
| <!-- transcript --> | <!-- e.g., recurring 1:1s --> | <!-- biweekly --> | |
| <!-- chatgpt_export --> | <!-- e.g., research chats --> | <!-- monthly --> | |

## 4. Lens Priorities

If `PORTAL_DATABASE_URL` is configured and lens scoring is active, list which dimensions matter most for THIS vault. Order is meaningful — first is highest priority.

1. **Fidelity** — source citations must be accurate and complete (TODO: keep or drop)
2. **Freshness** — content must reflect current state, not historical (TODO: keep or drop)
3. **Clarity** — readable by a non-expert in the domain (TODO: keep or drop)

## 5. Compile Profile Preferences

Per-stage LLM provider/model preferences. Defaults are fine for most projects; override only when you have a reason.

```yaml
# Sample overrides (uncomment + edit, then mirror to _meta/config.yaml):
# classify:
#   provider: anthropic
#   model: claude-haiku-4-5-20251001
# extract:
#   provider: anthropic
#   model: claude-sonnet-4-6
# compile:
#   provider: anthropic
#   model: claude-opus-4-7
# query:
#   provider: anthropic
#   model: claude-opus-4-7
# lint:
#   provider: anthropic
#   model: claude-sonnet-4-6
```

## 6. Deferred Items

Things explicitly NOT in scope for V1 of this vault. Use the same DI-NNN pattern as `docs/project_plans/deferred-items-backlog.md` if this project participates in that backlog; otherwise local IDs are fine.

| ID | Item | Reason | Revisit |
|---|---|---|---|
| | | | |

## 7. Success Signals

How you know the vault is delivering value (not just accumulating files):

- TODO: 2–3 concrete signals. Examples: "agent answers a recurring question with citations from this vault", "I stop re-Googling X because the vault knows", "lens fidelity ≥ 0.8 across `wiki/concepts/`".

## 8. Operating Notes

Anything project-specific that future agents (or future you) need to know. Free-form. Examples: naming conventions, special tags, recurring agent prompts.

---

*Generated by `meatywiki` skill bootstrap on {{BOOTSTRAP_DATE}}. Edit freely — this file is yours.*
````

---

## Notes for the Bootstrap Agent

- Write everything inside the outer `markdown` fence (the template body) to `{{VAULT_PATH}}/wiki-spec.md`.
- After substitution, the file should be ~70 lines.
- Section 1 (Intent) MUST be filled from the user's response to `{{INTENT_PROMPT}}`. If the user declines to answer, leave the prompt placeholder verbatim — do NOT generate intent yourself.
- Sections 3, 6 contain example table rows in HTML comments. Leave them; they show the user the expected shape.
- If `wiki-spec.md` already exists, do not overwrite. Offer a diff for review instead (see `plan.md` Step B5).
