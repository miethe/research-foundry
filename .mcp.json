{
  "mcpServers": {
    "rf-mcp": {
      "type": "stdio",
      "command": "python",
      "args": ["-m", "research_foundry.services.search_router.mcp_server"],
      "cwd": "${workspaceFolder}"
    }
  }
}
