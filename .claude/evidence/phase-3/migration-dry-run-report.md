---
title: "Phase 3 — Workspace Migration Dry-Run Report"
generated: 2026-07-07
workspace_root: "/Users/miethe/dev/homelab/development/research-foundry"
migration_target_workspace_id: "default"
enforcement_status: "INERT — not applied; pending Human Gate #1"
report_type: dry_run
---

# Workspace Isolation Migration — Dry-Run Report

> **READ-ONLY**: This report was generated by `dry_run()` in zero-write mode.
> No files were modified. No `draft.yaml` was touched. No `catalog.db` was altered.
> Zero-write guarantee is proven by unit test `test_dry_run_no_writes` (mtime + SHA-256 hash assertion).

---

## Raw Dry-Run Output

```json
{
  "total_drafts": 0,
  "drafts_missing_workspace_id": 0,
  "drafts_missing_created_by": 0,
  "total_catalog_items": 0,
  "target_workspace_id": "default",
  "caller_impact_summary": "Today there is 1 implicit workspace so 0 existing callers would be newly denied by the workspace isolation migration."
}
```

---

## Record Count Summary

| Metric | Value | Notes |
|--------|-------|-------|
| Total report drafts on disk | **0** | `reports/drafts/` directory does not yet exist |
| Drafts missing `workspace_id` | **0** | No drafts → nothing to backfill |
| Drafts missing `created_by` | **0** | No drafts → nothing to annotate |
| Total `catalog_items` rows | **0** | `catalog.db` not yet built (`SCHEMA_VERSION` → 3 triggers auto-create on next use) |
| Target `workspace_id` for backfill | `"default"` | Locked by OQ-B decision |

---

## Mapping to Synthetic Default Workspace

All **0** legacy records would be assigned `workspace_id = "default"`.

No per-`created_by` inference is performed — this is the correct, locked behavior (OQ-B). `created_by` remains `null` for any backfilled record, by design. This is documented as an expected permanent gap in the rollback runbook at `docs/dev/architecture/runbooks/workspace-migration-rollback.md`.

---

## Un-Migratable Records

**None.** There are no records with conflicting or unresolvable workspace identifiers. All fields that would be touched are null → `"default"` assignments, which are unconditional.

---

## Catalog Items Side

`catalog.db` does not yet exist. On first access after `SCHEMA_VERSION` is bumped 2→3 (already done in this worktree branch), the existing `_ensure_schema()` / `rebuild()` path will:
1. Auto-create a fresh `catalog.db` with the new DDL (which includes `workspace_id TEXT NOT NULL DEFAULT 'default'`).
2. Re-import all runs from disk, populating `workspace_id = "default"` for every row via `_base_row()`.

This is **100% reversible** by reverting the `SCHEMA_VERSION` bump in `catalog_service.py` and running `rf catalog rebuild`. The table is provably disposable (module docstring, existing `rebuild()` call; no canonical data lives there).

---

## `draft.yaml` Side (Durable Records)

No `draft.yaml` files exist in this workspace. If they had existed, the forward migration would have:
- Walked every `<workspace>/reports/drafts/<id>/draft.yaml`.
- For each with `workspace_id: null`: atomically rewritten it (temp file + `os.replace()`) to `workspace_id: default`.
- Recorded every touched record in the manifest at `.rf_state/migrations/<timestamp>-workspace-backfill.json`.

This half IS irreversible at the raw filesystem level without the manifest — which is exactly why a tested `rollback()` function and a migration manifest exist (implemented in WKSP-302).

---

## Reversibility Assessment

| Surface | Reversibility | Mechanism |
|---------|--------------|-----------|
| `catalog_items` schema change | **Trivially reversible** | Revert `SCHEMA_VERSION` constant in `catalog_service.py` → `rebuild()` |
| `draft.yaml` backfill | **Reversible via manifest** | `rf workspace rollback <migration_run_id>` restores `workspace_id`/`created_by` to prior values from the manifest; tested by byte-identical round-trip test |
| Enforcement flip | **Not applicable** | WKSP-304 has NOT been run; enforcement is INERT |

**Rollback safety note**: The rollback function never keys on the value `"default"` — it uses only the explicit `migration_run_id`'s manifest. Legitimate post-migration activity that creates `workspace_id="default"` records will never be touched by an earlier rollback run.

---

## Caller Impact Assessment

**0 existing callers would be affected by the backfill alone.**

The backfill (WKSP-303) is additive-only: it sets `workspace_id="default"` on previously-null records but does NOT change any request-handling behavior. The enforcement predicate (`require_workspace_scope()`) is currently in advisory mode (always returns `allowed=True`; logs mismatches but never blocks). A cross-workspace read still returns 200 after the backfill — proven by test `test_cross_workspace_read_still_succeeds_after_backfill`.

**The behavioral change (cross-workspace reads → 404) is gated on WKSP-304, which requires Human Gate #1 approval.**

---

## Estimated Blast Radius

| Scenario | Impact |
|----------|--------|
| Backfill runs, enforcement stays advisory | Zero user-visible impact |
| Backfill + enforcement flip (WKSP-304) | Any caller accessing records from a different workspace_id gets 404 instead of 200. Since this workspace has 0 workspaces beyond "default", the blast radius is 0 for any single-operator deployment. In a multi-tenant deployment, callers would need to use their assigned `workspace_id`. |
| Rollback after backfill | `draft.yaml` files restored to prior state; `catalog.db` auto-rebuilt on next access |
| Rollback after enforcement flip | Revert `auth.workspace_isolation: enforced` → `advisory` in `foundry.yaml` / restart |

---

## Dry-Run Execution Context

| Property | Value |
|----------|-------|
| Workspace root | `/Users/miethe/dev/homelab/development/research-foundry` |
| Executed by | phase-owner orchestrator (READ-ONLY Bash call) |
| PYTHONPATH | `<worktree>/src` (worktree code, not editable install) |
| Python binary | `.venv/bin/python` (main repo venv) |
| Zero-write guarantee | Proven by unit test `test_dry_run_no_writes` in `tests/unit/test_workspace_migration_service.py` |
| Test suite status | 21/21 pass (`.venv/bin/python -m pytest tests/unit/test_workspace_migration_service.py -q`) |

---

## Human Gate #1 — What This Report Covers

Per the phase doc, Human Gate #1 reviews:
1. **This dry-run report** — record counts, workspace_id/created_by distribution, predicted assignment. ✓ Provided above.
2. **WKSP-303 BackfillReport** — actual backfill outcome (must match dry-run prediction). ⚠️ WKSP-303 was implemented and tested against FIXTURES only; the real backfill has NOT been run (zero records means the two counts trivially match: both are 0).
3. **WKSP-302 rollback round-trip test** — must be green in CI. ✓ 21/21 pass including all round-trip and manifest-safety tests.

**Approval unlocks**: `rf workspace enforce --on` (WKSP-304). That command mechanically refuses to run without a recorded `.rf_state/migrations/<migration_run_id>-gate1-approval.json` artifact.

---

## Resume Steps After Human Gate #1 Approval

After the operator reviews this report and approves:

1. Create the gate approval artifact:
   ```
   .rf_state/migrations/<migration_run_id>-gate1-approval.json
   ```
   with fields: `operator`, `timestamp`, `dry_run_report_ref`, `backfill_report_ref`.

2. Resume the phase-owner with:
   > "Human Gate #1 approved. Apply the real migration and activate enforcement (WKSP-304 → WKSP-305 → WKSP-900)."

3. The orchestrator will dispatch:
   - `backend-architect` for WKSP-304 (enforcement flip)
   - `data-layer-expert` + `backend-architect` for WKSP-305 (0-leak regression suite)
   - `backend-architect` for WKSP-900 (FE contract test)

4. Both reviewer gates (`task-completion-validator` and `karen` isolation milestone) must sign off before the worktree is merged.
