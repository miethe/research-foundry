# Prior-Day Summary — {target_date}

## High-Level Summary

{summary_opening}

## Provenance Snapshot

- Classification: `{classification}`
- Current branch: `{current_branch}`
- Mainline commits in window: `{mainline_count}`
- Window commits observed: `{window_commit_count}`
- Merge commits observed: `{merge_count}`
- Branch divergence (`origin/main...HEAD`): `{branch_divergence}`
- Changelog hits: {changelog_hits}

## Visible Changes

{visible_changes_block}

## Caveats

{caveat_block}

## Suggested Next Passes

- Map notable work to plans / PRs / version headings
- Add try-it steps only for real visible surfaces
- Keep branch-local or docs-only follow-up explicitly labeled
