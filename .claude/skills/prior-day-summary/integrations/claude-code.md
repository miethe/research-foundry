# Claude Code Notes

- Invoke with `Skill("prior-day-summary")`.
- Follow the same workflow ordering as any other agent: date window → classification → visible-surface pass only if needed.
- Keep core routing in `SKILL.md`; do not duplicate the workflow docs in prompts.
