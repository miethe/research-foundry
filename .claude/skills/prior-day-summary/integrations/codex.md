# Codex Notes

- Open `SKILL.md` first, then load only the single matching workflow doc.
- Prefer running the bundled scripts over re-deriving the same git/changelog checks inline.
- Keep the answer concise by default; use the full workflow only when the user asks for visible changes, debt analysis, or visual companions.
